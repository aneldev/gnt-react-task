import * as React from "react";

export interface IListPageProps {}

export const ListPage: React.FC<IListPageProps> = () => {
  return (
    <div>
      <h3>ListPage (in progress)</h3>
    </div>
  );
};
