import * as React from "react";

export interface ITimelinePageProps {}

export const TimelinePage: React.FC<ITimelinePageProps> = () => {
  return (
    <div>
      <h3>TimelinePage (in progress)</h3>
    </div>
  );
};
